import { elements } from "../elements";

export default class ListaTimes {
  constructor(tiempo) {
    this.lista = [];
  }
  subirAlState(tiempo) {
    this.lista.push(tiempo);
  }
  mediaA05(tiempos) {}
}
